# YouTube Data API v3 金鑰(必要)。到 https://console.cloud.google.com/ 建立專案、
# 啟用「YouTube Data API v3」、在憑證頁面建立 API 金鑰後填入下方。
YOUTUBE_API_KEY=

# 留言翻譯(--engine claude 或 --engine auto)改用本機安裝的 Claude Code CLI
# (`claude -p`)執行,不需要 API 金鑰,但要先安裝 Claude Code 並執行 `claude login`
# 登入。偵測不到本機 claude 指令時,auto 會自動退回 Google 翻譯(免登入)。
