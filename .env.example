# YouTube Data API v3 金鑰(必要)。到 https://console.cloud.google.com/ 建立專案、
# 啟用「YouTube Data API v3」、在憑證頁面建立 API 金鑰後填入下方。
YOUTUBE_API_KEY=

# Anthropic Claude API 金鑰(選填)。--engine claude 或 --engine auto 時,若有設定此金鑰
# 會優先用 Claude 翻譯留言(品質較佳);未設定時 auto 會退回 Google 翻譯(免金鑰)。
ANTHROPIC_API_KEY=
