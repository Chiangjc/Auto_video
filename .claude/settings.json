{
  "permissions": {
    "deny": [
      "Bash(git commit:*)"
    ]
  },
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.command' | grep -qE '(^|[^A-Za-z0-9_./-])git([[:space:]]+[^[:space:];&|]+)*[[:space:]]+commit([[:space:]]|$|;|&|\\|)' && echo '{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"deny\",\"permissionDecisionReason\":\"git commit is disabled by project policy (.claude/settings.json). Ask the user to run it manually.\"}}' || echo '{}'",
            "statusMessage": "Checking git commit policy"
          }
        ]
      }
    ]
  }
}
