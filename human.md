# auto_video 說明(自己看用)

## 這是做什麼的
貼一個 YouTube 影片網址,自動下載影片、抓熱門留言、翻譯成中文、做成留言卡片疊到影片上,輸出成 9:16 短影片。

## 功能

- **下載影片**:貼網址就抓,可指定只抓某個時間區間(例如只要 30~90 秒)
- **抓留言**:用 YouTube 官方 API,依熱門度抓,可設定要抓幾則候選
- **篩選留言**:讚數門檻、字數範圍、擋掉髒話/廣告/劇透關鍵字
- **翻譯留言**:Claude 或 Google 二選一,語氣盡量貼近原文,不會亂加迷因梗
- **留言卡片**:黑底、頭像、帳號、時間、內容、讚數,長得像真的 YouTube 留言;可開雙語(原文+翻譯)
- **合成影片**:固定 1080x1920,上面標題、中間影片(自動裁成正方形置中)、下面留言卡片一則接一則播,秒數固定、無間隔
- **自動算留言則數**:依影片長度跟每則秒數,自動算出剛好播完的則數,篩選時多留 5 則備選
- **音量正規化**:合成完自動調到 -14 LUFS(YouTube 標準響度),可關閉
- **獨立音量工具**(`adjust_volume.py`):想手動調某支影片音量、不想重跑整條流程時用

## 兩種用法

- **命令列**(`main.py`):寫腳本、批次跑一堆影片用
- **網頁介面**(`webui/app.py`,開 `http://127.0.0.1:5001`):
  1. 貼網址、設定篩選條件 → 自動下載+抓留言+翻譯
  2. 檢視留言:可以取消勾選、改翻譯文字、拖曳調整順序、加標題
  3. 合成完成,顯示影片、頻道名稱、影片簡介

## 進度紀錄

### 2026-08-11
- 加上影片旋轉功能(向左/向右 90 度、180 度),合成前先轉再裁切,適合修正側錄跑偏的素材
- 加上留言拖曳排序(原本只有 ▲▼ 按鈕)
- 修掉 yt-dlp 下載偶爾 403 的問題(拿掉寫死的 player_client 設定,讓 yt-dlp 自己選)
- 整合 `adjust_volume.py`,合成完預設自動跑音量正規化
- 雙語卡片、音量正規化改成預設開啟
- 加上抓頻道名稱、影片簡介,合成完顯示
- 加上留言數量自動計算(依影片長度 + 每則秒數,不用手動填要抓幾則)
- 改成固定輸出 9:16(1080x1920):標題在上、影片置中裁正方形在中間、留言卡片在下
- 加上網頁介面(貼網址 → 檢視留言 → 完成)
- 加上 YouTube 下載區間(`--start`/`--end`)
- 加上 `.env` 自動讀取金鑰(之前要手動設環境變數)
- 第一版完成:下載影片 → 抓留言 → 篩選 → 翻譯 → 抓頭像 → 產生留言卡片 → 合成影片,CLI 跟網頁介面都有
